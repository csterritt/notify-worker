## Notifier Worker - A Cloudflare Worker for Sending Notifications via Pushover

This is a Cloudflare Worker that sends notifications via Pushover.

It requires a Pushover API token and user key to be configured in the Cloudflare Worker environment.
